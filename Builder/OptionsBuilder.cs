using Circuits.ConsoleInput.Options;
using System;
using System.Collections.Generic;
using System.Text;

namespace Circuits.ConsoleInput.Builder
{
    public class OptionsBuilder : IBuilder //TODO: actually fucking use this
    {
        /*private Dictionary<string, BaseCommand> _options;
        public void AddOption(string optionName, BaseCommand optionClass)
        {
            if(_options == null) _options = new Dictionary<string, BaseCommand>();
            _options.Add(optionName, optionClass);
        }

        public Dictionary<string, BaseCommand> GetOptions()
        {
            if (_options == null) return new Dictionary<string, BaseCommand>();
            return _options;
        }*/
    }
}
